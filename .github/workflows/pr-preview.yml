name: pr-preview

on:
  pull_request:
    paths:
      - '**.md'

jobs:
  call:
    uses: <adopter-org>/organisationos-foundation/.github/workflows/pr-preview.yml@v1  # pin to a released tag or commit SHA; bump via a two-approver Foundation PR that also rolls the tag
