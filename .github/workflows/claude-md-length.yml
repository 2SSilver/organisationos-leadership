name: claude-md-length

on:
  pull_request:
    paths:
      - '**/CLAUDE.md'

jobs:
  call:
    uses: <adopter-org>/organisationos-foundation/.github/workflows/claude-md-length.yml@v1  # pin to a released tag or commit SHA; bump via a two-approver Foundation PR that also rolls the tag
