name: format-gate

on:
  pull_request:

jobs:
  call:
    uses: <adopter-org>/organisationos-foundation/.github/workflows/format-gate.yml@v1  # pin to a released tag or commit SHA; bump via a two-approver Foundation PR that also rolls the tag
