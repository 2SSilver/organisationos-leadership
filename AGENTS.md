# AGENTS.md — Leadership repo

Cross-vendor agent definitions for this repo live in the Foundation repo at `<adopter-org>/organisationos-foundation/.github/agents/`. They are loaded by Claude via `additionalDirectories` (see `.claude/settings.local.json.example`).

Leadership-specific agent overrides (rare) would live in this repo's `.github/agents/` — currently none.
